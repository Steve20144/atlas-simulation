services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: tiltlab:local
    ports:
      - "8000:8000"
    volumes:
      - ./scenarios:/app/scenarios
    restart: unless-stopped
